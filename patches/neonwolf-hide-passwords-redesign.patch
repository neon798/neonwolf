diff --git a/browser/components/preferences/privacy.js b/browser/components/preferences/privacy.js
--- a/browser/components/preferences/privacy.js
+++ b/browser/components/preferences/privacy.js
@@ -768,6 +768,10 @@
 
     if (Services.prefs.getBoolPref("librewolf.hidePasswdmgr", false)) {
       document.getElementById("passwordsGroup")?.remove();
+      // Neonwolf: the upstream LibreWolf hide only removes the legacy
+      // #passwordsGroup. FF152 also renders a redesigned passwords
+      // setting-group; remove it too so no Passwords UI remains.
+      document.querySelector('setting-group[groupid="passwords"]')?.remove();
     }
 
     // Notify observers that the UI is now ready
